type Query {
    todos: [Todo]
}

type Todo {
    _id: ID
    title: String!
    content: String
    user: User
}

input TodoInput {
    title: String!
    content: String
    user: User
}

type Mutation {
    createTodo(input: TodoInput): Todo
}